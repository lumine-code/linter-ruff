{
  "name": "linter-ruff",
  "author": "lumine-code",
  "description": "A wrapper around the Python linter and formatter ruff.",
  "repository": "https://github.com/lumine-code/linter-ruff",
  "bugs": {
    "url": "https://github.com/lumine-code/linter-ruff/issues"
  },
  "keywords": [
    "python",
    "flake8",
    "isort",
    "pyflakes",
    "pycodestyle",
    "pep8",
    "formatter"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "lumine": ">=1.100.0 <2.0.0"
  },
  "main": "./lib/main",
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "activationHooks": [
    "language-python:grammar-used",
    "jupyter-view:grammar-used",
    "source.jupyter:root-scope-used"
  ],
  "activationCommands": {
    "atom-workspace": [
      "linter-ruff:toggle-state",
      "linter-ruff:toggle-noqa",
      "linter-ruff:lint-projects",
      "linter-ruff:lint-selected",
      "linter-ruff:global-pyproject"
    ]
  },
  "consumedServices": {
    "linter.registry": {
      "versions": {
        "^1.0.0": "consumeLinterRegistry"
      }
    },
    "busy-signal": {
      "versions": {
        "^1.0.0": "consumeBusySignal"
      }
    },
    "tree-view.selection": {
      "versions": {
        "^1.0.0": "consumeTreeViewSelection"
      }
    }
  },
  "providedServices": {
    "linter.provider": {
      "versions": {
        "1.0.0": "provideLinter"
      }
    }
  },
  "linter-package": true,
  "configSchema": {
    "state": {
      "title": "Linter state",
      "description": "Enable or disable the service of linter.",
      "type": "boolean",
      "default": true
    },
    "ruffCommand": {
      "title": "Ruff Command",
      "description": "Command to run ruff. Can be a path or a multi-word command like `py -m ruff`. Leave as 'ruff' to use the one in your PATH.",
      "type": "string",
      "default": "ruff"
    },
    "pyVersion": {
      "title": "Python version",
      "description": "The minimum Python version that should be supported.",
      "type": "string",
      "enum": [
        {
          "value": "",
          "description": "Default"
        },
        {
          "value": "py315",
          "description": "Python 3.15"
        },
        {
          "value": "py314",
          "description": "Python 3.14"
        },
        {
          "value": "py313",
          "description": "Python 3.13"
        },
        {
          "value": "py312",
          "description": "Python 3.12"
        },
        {
          "value": "py311",
          "description": "Python 3.11"
        },
        {
          "value": "py310",
          "description": "Python 3.10"
        },
        {
          "value": "py39",
          "description": "Python 3.9"
        },
        {
          "value": "py38",
          "description": "Python 3.8"
        },
        {
          "value": "py37",
          "description": "Python 3.7"
        }
      ],
      "default": ""
    },
    "select": {
      "title": "Rule selection: Select",
      "description": "Comma-separated list of rule codes to enable or `ALL` to enable all rules. Leave empty if you want use configuration files.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "ignore": {
      "title": "Rule selection: Ignore",
      "description": "Comma-separated list of rule codes to disable.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "fixable": {
      "title": "Rule selection: Fixable",
      "description": "Comma-separated list of rule codes to treat as eligible for autofix.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "unfixable": {
      "title": "Rule selection: Unfixable",
      "description": "Comma-separated list of rule codes to treat as ineligible for autofix.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "error": {
      "title": "Severity selection: Error",
      "description": "Comma-separated list of rule codes to treat as error type.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "warning": {
      "title": "Severity selection: Warning",
      "description": "Comma-separated list of rule codes to treat as warning type.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "info": {
      "title": "Severity selection: Info",
      "description": "Comma-separated list of rule codes to treat as info type.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "hint": {
      "title": "Severity selection: Hint",
      "description": "Comma-separated list of rule codes to treat as hint type.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "useNoqa": {
      "title": "Use noqa flag",
      "description": "Flag `# noqa` is used to silent linter messages per line.",
      "type": "boolean",
      "default": true
    },
    "addStar": {
      "title": "Unclassified flags marked by star",
      "description": "Add star to all flags which user do not classified yet.",
      "type": "boolean",
      "default": true
    },
    "allowMagic": {
      "title": "Allow magic commands",
      "description": "A bypass of magic commands e.g. `%timeit`, `_`.",
      "type": "boolean",
      "default": true
    },
    "deleteOnOpen": {
      "title": "Delete Project Lint on File Open",
      "description": "Delete project-wide lint messages for a file when it is opened in the editor. Restart needed.",
      "type": "boolean",
      "default": true
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.7.0",
    "prettier": "^3.9.6"
  }
}
